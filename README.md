# nodeAuth-webApp

In the project directory, first run:
>make sure your're in server folder!

```
npm install
> require nodemon installed
```
 then you can run:
 
```
 nodemon
```
 or:
```
 npm start
```
